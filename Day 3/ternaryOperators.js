let isRaining = true
isRaining
    ? console.log("It is raining")  //Si la condicion isRaining es true
    : console.log("It is NOT raining uwu")  //Si la condicion is raining es false
isRaining = false

isRaining
    ? console.log("It is raining")  //Si la condicion isRaining es true
    : console.log("It is NOT raining uwu")  //Si la condicion is raining es false

let number = 5
number > 0
    ? console.log(`${number} is a positive number`)
    : console.log(`${number} is a negative number`)
number = -5

number > 0
    ? console.log(`${number} is a positive number`)
    : console.log(`${number} is a negative number`)